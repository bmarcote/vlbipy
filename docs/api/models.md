# Models

Core data models for VLBI observations. All domain objects are plain dataclasses with no backend dependencies. They are serializable and can be reconstructed from metadata dictionaries.

::: vlbipy.models
